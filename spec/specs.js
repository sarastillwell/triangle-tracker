describe('equilateral', function() {
  it ("is true if all sides are the same", function() {
    expect(equilateral[2,2,2]).to.equal(true);

  });

  it("is false if one side is different", function() {
    expect(equilateral[2,2,1].to.equal(false);


  });
});
